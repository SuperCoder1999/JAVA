package chapter14collection.set_.set;

/*
    一：Set接口基本介绍
        和List接口一样Set接口也是Collection的子接口。因此，常见方法和Collection一样

    1. 无序（添加和取出的顺序不一致），没有索引
    2. 不允许重复元素，所以最多包含一个null
    3. JDK API中的Set接口的实现类有其他的
    4. 遍历方式和Collection的遍历方式一样，因为Set接口是Collection接口的子接口
        1）可以使用迭代器
        2）可以使用增强for循环
        3）不能使用索引的方式来获取【没有索引，所以不能】

 */

public class SetIntroduce {
}
